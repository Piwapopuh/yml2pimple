parameters:
  name: Gonzalo

services:
  App:
    class:     \test\fixtures\App
    arguments: [@Proxy, %name%]

  Proxy:
    class:     \test\fixtures\Proxy
    arguments: [@Curl]

  Curl:
    class:     \test\fixtures\Curl
